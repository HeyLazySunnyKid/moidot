ansible-playbook -i localhost, -c local /dev/stdin <<'EOF'
- name: "Install packages"
  hosts: localhost
  tasks:
    {{ range .pkgs }}
    - name: Install package {{ . }}
      package:
        name: {{ . }}
    {{ end }}
EOF
